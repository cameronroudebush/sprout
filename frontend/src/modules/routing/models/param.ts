/** Supported params for the router service */
export enum Params {
  /** Helps keep track of the page we were on before a refresh so we end up back at what we were looking at */
  lastUrl = "lastUrl",
}

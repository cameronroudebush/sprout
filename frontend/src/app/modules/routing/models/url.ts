/** Enum containing the paths for the available routes in this app */
export enum RouteURLs {
  dashboard = "dashboard",
  /** Default route should always end back up here */
  default_route = "dashboard",
}

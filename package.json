{
  "private": true,
  "scripts": {
    "install:all": "npm i && npm i --prefix ./backend && npm i --prefix ./common && npm i --prefix ./frontend --verbose",
    "build:docker": "ts-node --project ./backend/tsconfig.build.json ./scripts/build.docker.ts",
    "prettier:check": "prettier . --check",
    "prettier:write": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2"
  },
  "prettier": {
    "printWidth": 160
  }
}
